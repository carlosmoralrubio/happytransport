import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [
    react({
      jsxRuntime: 'automatic', // ensures React is injected automatically into every JSX file
    }),
  ],
  build: {
    outDir: 'dist',
  },
})